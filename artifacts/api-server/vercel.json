{
  "version": 2,
  "builds": [
    {
      "src": "index.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "buildCommand": "pnpm run build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile"
}
